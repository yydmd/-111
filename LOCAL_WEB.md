# 本地网页版使用说明

## 统一自动预约（v11）

一个管理页、一套预约流程。不同账号独立登录和保存会话；无需验证码时全自动预约，只有登录失效、账号确认或官方验证码需要你操作。

1. **添加账号**：填写名称和超星账号；密码可以不填，直接在官方窗口使用密码、短信或扫码登录。无需先建立预约计划。
2. **检查登录／提前登录**：按钮会访问官方账号资料确认当前登录有效。有效时显示最近检查时间并最小化窗口；失效时自动打开登录页。扫码或短信首次无法自动对应账号时，在资料页核实后点击运行记录的“确认是本计划账号”。网络异常只显示检测失败，不清除会话。
3. **保存并启用预约计划**：配置阅览室、候选座位、完整时段、执行时间与星期。无需选择技术模式，也无需先探测、演练或试约。登录完成不代表目标日期已经开放。
4. **自动准备**：开抢前 10 分钟检查登录，前 30 秒准备选座页；当前账号的登录任务会与预约在同一窗口交接。跨午夜按预约执行日计算日期和星期。
5. **到点自动预约**：按服务器时钟等待；无需验证码时直接提交。有验证码时恢复原窗口、尝试置前、声音及任务栏提醒。完成后系统读取同一会话的官方记录，精确核对日期、阅览室、座位与完整时段。
6. **查看结果**：每个候选最多一次自动预约操作，官方明确拒绝后才换下一座位。未知结果不重复提交；在原运行记录点“只读核对预约”。“停止接管”停止后续操作，可能已提交时先做有限核对。

平时窗口最小化。手动登录检查完成后，空闲窗口最多保留 10 分钟；期间预约复用该窗口，之后自动关闭并保存会话。自动预约从准备到结果核对期间保持同一会话。Windows 必须登录、解锁并保持唤醒；官方可能提前撤销登录，因此准备时和开抢前会复查，不能保证会话永久有效。

同账号任务串行，重复检查复用已有任务；不同预约明确排队，不把正在登录当作“预约已经执行”。最多 4 个账号活动任务并行。正式等待最多持续到开抢后 5 分钟，手动预约从开始计时；可能已提交时再做最多 20 秒核对。服务重启不重放中断的提交，账号状态改为待复查。

账号资料使用 `data/browser-profiles/account-<id>/` 独立目录。`session.dpapi` 使用当前 Windows 用户的 DPAPI 加密，恢复时必须匹配账号身份。数据库仅保存登录检查状态、时间和简短原因，不保存 Cookie、验证码或完整请求。`data/` 不提交版本库。

如首次使用尚无浏览器组件，在项目目录运行 `powershell -ExecutionPolicy Bypass -File .\scripts\install_browser.ps1`。Windows 优先使用系统 Edge，其次 Chrome。组件缺失或桌面不可交互时明确失败，不回退到旧接口提交。

辅助诊断收在计划卡片的折叠区域。“检查预约页面”只读，不提交预约，未开放时仅报告原因，不影响提前登录。旧 API 模式字段仍接受以兼容旧页面，但正常管理页和定时预约统一使用浏览器流程。

升级至 v11 前进行 SQLite 一致性备份（包含 WAL）。账号、计划、启用状态、历史与通知配置保持；现有计划执行流程统一为浏览器。回退软件版本前先停用计划、结束活动任务并备份当前数据库，不盲目恢复旧库重放预约。

开发验证：`python -m pytest tests/test_core.py tests/test_browser.py -q`。浏览器测试使用模拟官方页面，不向超星提交预约。

## 启动

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
.\.venv\Scripts\python.exe main.py -m serve
```

如需临时使用守护进程，可直接运行 `scripts/watchdog.py`；日常使用建议安装下面的计划任务。

浏览器访问 <http://127.0.0.1:8787/>。首次启动会在 `data/app.db` 创建 SQLite 数据库。

也可以运行 `powershell -ExecutionPolicy Bypass -File scripts/install_task.ps1 -StartNow`，安装守护式开机自启任务并立即启动。守护进程会通过 `/health` 检查服务和调度器状态，异常退出或假死时自动重启。如果创建计划任务被系统拒绝，可改用当前用户启动目录：`powershell -ExecutionPolicy Bypass -File scripts/install_startup.ps1`。

## 历史命令行脚本（不属于统一管理页流程）

原有命令仍可用：

```powershell
python main.py -m debug -u config.json
python main.py -m reserve -u config.json
python main.py -m room -u config.json
```

新网页版本使用独立 SQLite 配置，不会修改或删除原来的 `config.json`。

## 安全边界

- 服务只监听 `127.0.0.1`，不应通过端口转发暴露到局域网。
- 不要把 `data/`、账号密码、Cookie 或运行日志提交到 Git。
- 检测到点选验证码、非法预约、限流或安全验证超时会停止本次任务。
