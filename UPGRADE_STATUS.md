# 统一自动预约 v11 交付记录

更新：2026-09-08。当前需求是一套通用账号登录和自动预约流程，不再要求按账号或计划选择技术模式。

## 已交付

- 所有账号提供独立的“检查登录／提前登录”，不需要先创建计划，添加账号可不保存密码。
- 恢复加密会话后访问官方账号页检查有效性；失效时显示官方登录窗口，网络故障保留资料并报告检测失败。
- 同账号使用单一持有线程和浏览器会话，登录检查与预约有序交接；重复检测复用任务，不吞掉定时预约。
- 默认提前 10 分钟检查登录、提前 30 秒准备页面，跨午夜正确计算星期和目标日；等待最多到开抢后 5 分钟。
- 平时最小化，需要登录、账号确认或验证码时恢复、尝试置前、任务栏闪烁并发出本地提示。
- 无验证码时自动提交并核对官方完整记录；不会误发人工接管提示。未知结果不重复提交，只有官方明确拒绝后才顺序换座。
- 每账号独立资料目录和 DPAPI 会话文件；手动检查后的空闲窗口保留 10 分钟，预约期间保持会话。
- 管理页移除模式选择、正式试约和模式切换按钮；页面诊断放入辅助区域。
- v11 迁移统一旧计划的正常执行流程；兼容旧请求中的模式字段但不恢复 API 自动提交。重启后登录状态改为待复查。
- 第二条“肖”计划按用户要求保持停用，账号与历史记录保留。

## 测试和实机证据

- 最终完整回归 167 项通过（`data/qa/v11-tests.xml`）；日期展示修正后，追加 2 项定向回归通过（`data/qa/v11-ui-tests.xml`）。
- 计划卡片按调度器的下一次执行日计算目标日；停用计划明确显示已停用，不再推算虚假执行时间。
- 修复账号未登录时窗口先被最小化的问题；全新或失效账号会直接显示官方登录页。修复账号浏览器空闲退出时的 Playwright 清理错误。
- 修复超星将相邻预约合并为一条较长记录时的结果误判：同一阅览室、候选座位的记录完整覆盖目标时段即可确认成功；部分覆盖或其他座位仍标记冲突。实机只读核对 #349 已将误判的 #348 纠正为成功，核对过程未再次提交。

- 回归测试覆盖账号无计划登录、有效/失效/错账号/断网、同窗口复用、无需验证码自动完成、不同账号隔离、同账号排队交接、重复回调、验证码暂停、窗口恢复、跨午夜准备、队列参数快照、迁移和前端控件。
- 最终服务重载后，实机任务 #340 再次确认登录 READY，管理页日期校验通过、无页面错误；证据 `data/qa/v11-final-live.json`。
- 实机任务 #335、#336：官方确认现有账号登录有效，没有要求重新登录。连续检查保持同一个 Edge 浏览器主进程 7660。
- 登录检查后执行只读计划检查 #337，仍复用进程 7660。实机复查 #338 能读取目标日期和时段，因目标时段包含官方不可选时间而停止，未提交预约。
- 历史真实任务 #326 已验证人工验证码完成后成功预约并精确核对官方记录。v11 新增的无验证码全自动提交和登录到预约会话交接使用模拟官方页面验证，未额外制造真实预约。
- 实际管理页已检查：每个账号显示登录检查按钮，无技术模式下拉框、无正式试约按钮，无 JavaScript 页面错误。截图在 `data/qa/v11-management-live.png`。

## 数据与安装

- 已安装数据库版本 11。自动备份：`data/backups/app-before-v11-20260908-201325.db`。
- 对照备份逐字段核对：2 个账号、3 条计划（除统一执行流程字段）、9 个座位项、334 条升级前历史记录、3 项设置完整保留；启用状态不变。
- 核对报告：`data/qa/v11-migration-audit.json`，不含密码或 Cookie。
- 管理服务、数据库和调度器健康检查均正常；保留现有 Windows 用户登录后的守护启动入口。

使用说明见 [LOCAL_WEB.md](LOCAL_WEB.md)。官方会话可能过期，已登录显示的是最近一次官方检查结果；每次预约准备还会重新核实。座位被占用、未开放或官方拒绝时不能承诺预约成功。
