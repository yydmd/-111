# 2026-09-11 漏跑修复与验收

范围：`codex/risk-control-compat`，基线 `32882e8`。只改当前项目；旧目录 `D:\codex工作区域\图书馆抢座位` 和 `local-web-refactor` 分支保留，不启动、不删除。

## 07:30 没动静的原因

正式定时器提前 10 分钟唤醒。07:28–07:29 保存 07:30 的测试计划时，07:20 的预热触发点已经过去，正常应由保存后的补跑逻辑接管。

历史任务 #388（目标日 9 月 11 日）被旧恢复逻辑标成 `INTERRUPTED_NEEDS_VERIFICATION`，但 `possibly_submitted=0`、浏览器快照存在、没有放行提交的尝试记录。补跑逻辑按账号和目标日拦截它，却不向页面说明原因，导致没有创建本次定时任务。#389 是相同性质的历史误标。

#390 是 07:28 的浏览器演练，结果为 `BROWSER_PREVIEW_READY`，不曾提交预约；它不能证明 07:30 定时执行成功。回归测试也覆盖了尚未结束的演练不能吞掉新定时任务。

## 修复清单

| 问题 | 处理与边界 |
| --- | --- |
| 历史未提交记录误拦补跑 | 只修复浏览器快照、未标记可能提交、没有已提交尝试的误标行；其余未知提交继续拦截 |
| 补跑失败无提示 | 提供 `schedule_status`，显示关联任务编号、拦截或入队失败原因；失败数不再计入成功入队数 |
| 演练与定时混合去重 | 按任务种类、预约意图和开抢时间去重；同一轮定时/补跑仍合并 |
| 入队异常留在 PENDING | 工作线程接收失败时标记 `ENQUEUE_FAILED`，避免永久排队假象 |
| 排队不能取消 | 取消与启动使用条件更新原子竞争；已取消任务不可再被工作线程启动，也不可被同轮补跑复活 |
| 排队登录取消后仍显示检查中 | 同步清除账号 CHECKING 状态 |
| 跨午夜日期错一天 | 快照、补跑和页面共用开抢日计算；00:05 的预热属于前一日 23:55 |
| UI 显示不存在的今日任务 | 只有确实存在该开抢时间的活动定时记录才显示当轮，否则显示真实 cron 下一次；账号停用也明确显示 |
| 前端下次执行不刷新 | 轮询同步更新计划时间、目标日和补跑状态，不重置正在编辑的表单 |
| 官方读取请求误拦 | 精确放行 GET `getdrawseat`、GET `seatgrid/roomid`；取消、签到、续约等写操作继续拦截 |
| 首座时段禁用就整轮退出 | 明确区分候选不可用与页面结构不支持；不可用候选记入审计，继续下一座 |
| 混合风控/限流提示误分类 | 风控词优先；“异常操作，请稍后再试”立即停止，不继续换座 |
| 占用提示未识别 | 增加“该时间段已被占用”的明确拒绝判定 |
| 刷新重试授权过宽 | 必须收到官方明确拒绝、重新加载并验证页面后才解锁同座重试；闸门与执行器都限制一次 |
| 迟到的刷新提示拖满核对窗口 | 核对期间收到明确刷新拒绝即返回刷新流程，不再等完未知结果核对窗口 |
| 意外关窗被当作主动取消 | 独立识别 `BROWSER_WINDOW_CLOSED`，使既有的安全恢复一次逻辑真正可达；未知提交不恢复重发 |
| 时钟过期同步阻塞 | 缓存读取、后台单飞校准、只读取响应头；当轮浏览器等待使用服务器对齐后的单调时钟截止时间 |
| 缺少开抢延迟证据 | 浏览器审计增加开抢到点击、开抢到请求的耗时；不保存 token 或请求正文 |
| 脱敏吞掉后续参数、误伤 fidEnc | 精确匹配键边界并保留 `&` 参数结构；比较检查点消息前先脱敏，恢复两秒提交节流 |
| 损坏的通知密钥导致 500 | 统一转换为可读的校验错误，可重新填写或关闭通知 |
| 通知 HTTP 200 被当作业务成功 | 校验各通知平台的 JSON 业务结果；错误响应不回显密钥或正文 |
| 独立验收遗留数据库目录 | 新测试使用临时目录，释放数据库连接后清理；原有 68 个 `uc-*` 目录不删除 |
| Windows mutex ctypes 声明缺失 | 显式声明 HANDLE/参数类型，使用线程本地 last-error；这是加固，不将未证实的句柄截断当作本次根因 |
| 重复常量和星期 locale 耦合 | 共用预热常量与整数星期映射，不依赖 `%A` 的语言环境 |

## 对工程师结论的修正

- 生产模式固定浏览器是 v11 的架构选择，不应为了恢复旧竞速功能而重新打开 API 并发提交。旧 `fire_accept_calibration`、volley、racer 自愈不属于当前浏览器执行链；已明确标注文档和独立验收的适用范围。
- 原有测试并非全部无用：它们仍验证旧 API 与共享逻辑，但不代表浏览器生产验收，更不保证实战抢座成功。
- 同一账号的浏览器任务由账号线程串行处理；存在排队等待，但不能据此断言必然产生 60 秒账号锁超时。
- `close_account` 正常路径在账号线程 `finally` 释放上下文，未复现“线程退出后浏览器必然泄漏”；不做缺少证据的重写。
- 不带明确拒绝正文的提交 303、断网和结果未知仍只读核对，不能推断“失败”后盲目再提交。

## 验证方式

最终结果：`test_core.py` 101 项、`test_browser.py` 119 项、事故回归 21 项，共 **241/241 通过**（128.55 秒）；独立旧 API/共享逻辑验收 **43/43 通过**。`git diff --check`、`pip check` 通过。只有测试客户端的一条 AnyIO 弃用提示，不影响本次结果。

```powershell
.venv\Scripts\python.exe -X utf8 -m pytest tests/test_core.py tests/test_browser.py tests/test_regressions_20260911.py -q -p no:cacheprovider
.venv\Scripts\python.exe -X utf8 tests/upgrade_check.py
.venv\Scripts\python.exe -X utf8 scripts/verify_local_ui.py
```

新增 HTTP 测试使用隔离 SQLite、模拟 07:29 和假的任务接收器，覆盖管理 API 保存到持久化排队的完整链路。浏览器集成测试使用本地模拟页面/响应，覆盖禁用首座、占用换座、迟到刷新、最多一次刷新、未知结果不重发等场景。`httpx==0.28.1` 已列入开发依赖。

本轮 `.pytest_bugfix_*` 临时测试目录的自动删除被环境策略拦截，因此保留诊断文件并加入 Git 忽略规则；没有改用其他方式删除。旧工程师记忆文件和旧验收目录均保留。

管理页只读验收脚本拒绝所有非本地请求和非 GET 请求，不点预约、登录、通知测试按钮。已确认 3 个计划与 3 张计划卡一致、时间与账号启用状态匹配、无浏览器脚本/控制台错误；脱敏截图位于 `data/qa/bugfix-20260911-management.png`。端到端验收技能促使额外检查了页面、API、持久化和页面回显，而不只检查单元测试。

## 部署与恢复点

- 修复前数据库备份：`data/backups/pre-bugfix-20260911-080910.db`，SQLite 完整性检查通过。
- 08:09 恢复当前分支看门狗和本地服务；健康检查 `status=ok`、数据库/调度器正常、活动任务为 0。
- 启动恢复已修正 #388、#389；#390 演练记录保持不变，没有新增真实预约记录。
- 登录自启动快捷方式只指向当前目录；未发现旧分支服务进程。
- 保留配置：计划 #4 下次 2026-09-11 19:00、目标日 9 月 13 日；计划 #11 仍启用，下次 2026-09-12 07:30、目标日 9 月 12 日；计划 #7 仍停用。
- 未修改账号密码、预约时段、候选座位、启用开关或通知配置。通知仍未配置，不能期待手机推送。

## 验收边界

本次没有向学校提交预约，不把模拟成功当作实战成功。学校页面变化、座位被其他人抢先、登录过期、人工验证码、电脑休眠/锁屏及平台风控仍可能导致预约失败。必须保持电脑可用，并在需要时完成官方验证。

通知结果判定参考：[Server 酱官方 SendKey 文档](https://sct.ftqq.com/sendkey/)、[Bark 官方服务仓库](https://github.com/Finb/bark-server)、[企业微信机器人接口文档](https://developer.work.weixin.qq.com/document/path/91770)。
