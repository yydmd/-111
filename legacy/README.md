# 已归档的旧执行入口

旧版 `main.py -m reserve/debug/room` 与 GitHub Actions 定时任务已停止支持。
它们会绕过本地 Web 服务的目标日参数验证、单次候选策略和提交后核验，不能用于生产预约。

唯一受支持的生产入口是本地守护服务：`启动图书馆抢座.bat`，随后打开
`http://127.0.0.1:8787/` 管理账号、计划、预检与运行记录。
