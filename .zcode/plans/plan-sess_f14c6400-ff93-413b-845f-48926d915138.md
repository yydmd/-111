# 全面 Bug 修复计划（P0+P1+P2，共 17 项）

三路并行代码审查 + 日志/数据库实证（data/logs/service.log、data/app.db run #213/#221/#227/#229）已锁定根因。所有修复配回归测试，全量跑现有 72 个测试保证 problems.txt 已修复的 4 项不变量不被破坏（TOKEN_STALE 不计预算、racer 先对齐再取页、松弛刷新、终局如实报告）。

## P0 — 01:29 计划未执行根因

**1. 补跑去重过宽（app/scheduler.py:96-104）**
- `already_created` 查询增加状态过滤：仅 PENDING/RUNNING 在途 run 视为已占用（当天 FAILED/历史记录不再吞掉补跑）。真实重复预约已有 SUCCESS 指纹查重 + 服务端核实两层安全网，无需此去重兜底。
- 回归测试：当天已有同目标日 FAILED scheduled run → 补跑必须入队；已有 PENDING → 不重复。

**2. "下次自动执行"显示与调度器脱节（app/web.py + app/templates/index.html:72）**
- `/api/plans` 每计划附后端计算的 `next_fire_at`（取自 APScheduler job 的 next_run_time + LEAD_SECONDS 换算）；前端优先显示后端值，浏览器推算仅作后备。

## P1 — 高危

**3. 串行重试复用旧 token（app/service.py:928）**：`pre_resolved = None` 移入 except 分支——任何失败后的重试必须重新取页（problems.txt 事故的单座位残留路径）。测试：单座位 303 后第二次 submit 必须重新 resolve。

**4. 串行兜底不轮换座位（app/service.py:915-918）**：优先选择本轮尚未试过的存活座位轮换；保持 dead_seats 绝不重试、预算记账不变。测试：整队 303 后刷新预算分布到 097/098/099。

**5. clock 校准无时限（app/clock.py:66-101 + service.py:350）**：`refresh()` 增加 `budget_seconds`（dense≈8s），探测循环检查耗时，超预算用现有样本结算；`_scheduled_fire_epoch` 传入与 deadline 关联的预算。

**6. run_time 00:00–00:29 不支持（web.py 校验 + service.py:392）**：保存时 422 拒绝并说明原因；`_poll_until_open` 循环加 deadline 检查作纵深防御（超限抛 DEADLINE_EXCEEDED）。

**7. 停用/启用清空已存选座参数（web.py:257-274 + index.html:95）**：PATCH 改部分更新语义（字段 Optional，仅显式携带才更新）；`select_context_checked_at` 仅在参数实际变化时重置；前端 togglePlan 只发 `{enabled}`。测试：PATCH {enabled} 后 select_params_json 不变。

## P2 — 中危

**8. 未知重定向误判风控（chaoxing_client.py:252）**：改为可重试新码 `HTTP_REDIRECT`（入 _RETRYABLE_CODES + 前端文案）；登录/安全标记与 429 分类不变。
**9. poller 旧 token（service.py:391-397）**：`latest` 记时间戳，fire 时刻若已过旧（>1s）再取一次，失败才回退旧页。
**10. 措辞分类（chaoxing_client.py:730-739）**："频繁"移出风控 → 可重试 `RATE_LIMITED`；303 短语补充"请刷新后重试"等变体。
**11. escapeHtml 引号注入（index.html:65）**：补转义 `"` 与 `'`。
**12. 保存防双击（index.html:93）**：保存进行中禁用按钮，finally 恢复。
**13. misfire_grace_time 15→90（scheduler.py:17）**：与 STARTUP_CATCHUP_SECONDS 对齐。
**14. SQLite 并发（db.py:166）**：连接事件监听器设 `journal_mode=WAL` + `busy_timeout`。
**15. probe/run 无服务端防重复（web.py）**：同账号已有 PENDING/RUNNING 时返回 409（不影响 confirm-duplicate 路径）。
**16. waitForRun 出错后按钮永久禁用（index.html:96）**：错误路径也调用 onDone。
**17. clock 全局 Session 线程安全（clock.py:42-44）**：改为 threading.local 每线程独立 Session。

## 收尾

- **problems.txt**：按现有格式新增 2026-09-03 "01:29 计划未执行"事故条目（根因/修复/验证），并附全面排查清单——本轮已修 17 项 + 未修低危已知问题（迟到提交核实、重登闩锁、passport2 全文匹配误伤、legacy 无超时等约 25 项）留待后续。
- **验证**：`.venv/Scripts/python.exe -m pytest tests/test_core.py` 全量通过（72+新增）；`tests/upgrade_check.py` 新增场景（补跑入队、单座位刷新重试、座位轮换、HTTP_REDIRECT、PATCH 保留参数）；启动 web 服务手动 smoke（启停不清参数、下次执行时间显示后端值）。

涉及文件：app/scheduler.py、app/web.py、app/service.py、app/clock.py、app/chaoxing_client.py、app/db.py、app/templates/index.html、tests/test_core.py、tests/upgrade_check.py、problems.txt。不提交 git（保持当前未提交状态风格）。